# Council extracts — working rules

> **EXAMPLE content.** The `ST-9547-2026_*.md` files here are the worked compromise-version extract set
> from reference file 2025/0360, kept to show the per-version structure. Each new Council version gets its
> own identically-named set (`tracker.yaml` `extract_slices`). Replace with your file's texts; produce new
> sets with the `transcribe-council-extract` skill. See [`../../SETUP.md`](../../SETUP.md).

Before adding or editing any file in this directory, read
[`_TRANSCRIPTION_GUIDE.md`](./_TRANSCRIPTION_GUIDE.md) and follow it. It is the canonical
standard for transcribing Council compromise-text extracts (source of truth, consolidated
reading, faithfulness rules, structure/anchors, header block, cross-links).
